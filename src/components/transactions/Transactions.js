import React, { Component } from 'react';

export default class Transactions extends Component {
    render() {
        return (
            <div>
                {/* {this.props.transactions.map((data, key) => {
                    return (
                        <div key={key}>
                            {data}
                        </div>
                    );
                })} */}
            </div>
        )
    }
}